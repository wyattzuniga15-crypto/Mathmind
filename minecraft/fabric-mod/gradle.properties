# ---------------------------------------------------------------------------
# CHECK THESE FOUR LINES BEFORE BUILDING.
#
# fabric_version below is confirmed working for 1.21.11. The others are from
# https://fabricmc.net/develop/ — if a build fails resolving dependencies, that
# page has the correct values for whichever Minecraft version you target.
# ---------------------------------------------------------------------------
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.1
loader_version=0.17.2
fabric_version=0.141.6+1.21.11

# Resolved from Maven Central by the build workflow.
anthropic_version=2.9.0

# These rarely change.
loom_version=1.11-SNAPSHOT
mod_version=1.9.1
maven_group=com.orbital
archives_base_name=orbital-arsenal

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
